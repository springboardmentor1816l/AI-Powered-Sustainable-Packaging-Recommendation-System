# Experiment Reproducibility

To ensure reproducibility:

- Fixed random seed is used across all splits
- Train-test split parameters are documented
- Cross-validation configuration is saved
- Dataset versions are fixed

All experiments can be reproduced using saved metadata and artifacts.
