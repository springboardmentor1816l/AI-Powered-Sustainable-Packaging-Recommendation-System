version: "3"

services:
  backend:
    build: .
    ports:
      - "5000:5000"

  database:
    image: postgres
    environment:
      POSTGRES_DB: ecopackai
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
